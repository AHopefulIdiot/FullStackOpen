```mermaid
sequenceDiagram
    participant browser
    participant server
    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/spa
    activate server
    server-->>browser: HTML document
    deactivate server
    browser->>server: GET the CSS, JS and data.json file
    activate server
    server-->>browser: the CSS, JS and data.json file
    deactivate server
    note right of browser: the browser draws the notes
```
